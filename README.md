# Football Predictor

Dixon–Coles match grid. The host installs `penaltyblog` from `requirements.txt` at build time.

Local:

```bash
source .venv/bin/activate
uvicorn app:app --reload --port 8000
```
